{
  "name": "twitter-trends-analysis",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "fs": "0.0.1-security",
    "http": "0.0.0",
    "path": "^0.12.7",
    "socket.io": "^2.1.0",
    "twit": "^2.2.9"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm install && node server.js",
    "client": "cd src && npm start"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
